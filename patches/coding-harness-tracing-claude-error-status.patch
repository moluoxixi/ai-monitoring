diff --git a/tracing/claude_code/hooks/handlers.py b/tracing/claude_code/hooks/handlers.py
index 837e1d6..3456259 100644
--- a/tracing/claude_code/hooks/handlers.py
+++ b/tracing/claude_code/hooks/handlers.py
@@ -338,6 +338,8 @@ def _handle_post_tool_use_failure(input_json: dict) -> None:
         attrs,
         SERVICE_NAME,
         SCOPE_NAME,
+        status_code=2,
+        status_message=redacted_error or "Tool failed",
     )
     send_span(span)
 
@@ -1132,6 +1134,8 @@ def _handle_stop_failure(input_json: dict) -> None:
         attrs,
         SERVICE_NAME,
         SCOPE_NAME,
+        status_code=2,
+        status_message=redact_content(env.log_prompts, error_details) or error_type or "Stop failed",
     )
     if send_span(span) is False:
         return
